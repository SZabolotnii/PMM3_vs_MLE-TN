# TODO по репозиторію після скорочення статті до 10 сторінок

Контекст: `submission/SZabolotnii_ITEST_paper_10p.docx` зроблено з
`submission/SZabolotnii_ITEST_paper+++ref.docx` вручну (правки XML), а не з R-пайплайну.
Перелік самих правок — у `submission/CHANGES_14p_to_10p.md`.
Нижче тільки те, що стосується коду й репозиторію.

---

## 1. Супплемент (блокує подачу)

Текст статті двічі посилається на supplementary material, якого ще не існує.
Треба зібрати й отримати DOI, потім вставити посилання в docx.

Джерела вже є, нічого рахувати наново не треба:

| Що | Звідки |
|---|---|
| Повна таблиця Монте-Карло, 24 сценарії × 4 методи | `results/tables/T2_monte_carlo.csv` (96 рядків) |
| Теоретичні характеристики TN | `results/tables/T1_theoretical.csv` |
| Робастність при misspecification | `results/tables/T6_misspecification.csv` |
| Збіжність для бімодальних TN (колишня Табл. 6) | `results/tables/T7_bimodal_test.csv` |
| whiteside + чотири інші реальні датасети | `results/tables/real_data_all_candidates.csv`, `real_data_bootstrap.csv`, `real_data_loo.csv` |

Дії:
- [ ] зробити `supplement/` з цими CSV + коротким README (опис колонок, seed 20260312, версії пакетів)
- [ ] залити на Zenodo, отримати DOI
- [ ] вставити DOI у два місця в docx: розд. 3.1 («the full numeric table is provided
      as supplementary material») і в кінці розд. 3.2

---

## 2. Полагодити генерацію рисунків

`scripts/08_generate_paper_figures.R` — заголовки всередині графіків обрізаються при
експорті: `save_fig()` за замовчуванням 14×9 см, а рядки заголовків довші.
У `Fig1_TN_densities.png` видно «shape parar», у `Fig2_efficiency_curve.png` — «Var(OL».

Для Springer LNNS заголовок усередині графіка взагалі зайвий — підпис іде під рисунком.

- [ ] прибрати `title =` і `subtitle =` з усіх `labs()` (рядки 52, 95, 128–129, 156–157, 196–197)
- [ ] зробити з Fig1 + Fig2 один двопанельний рисунок (patchwork: `p1 | p2`),
      експорт ~24×7 см — зараз я склеїв їх через PIL постфактум, у репо цього немає
- [ ] Fig3_g3_convergence і Fig4_ARE_comparison — лишити як є, але експортувати
      без заголовків; у статті вони вставлені шириною 10.0 см
- [ ] Fig5_iris_residuals у статті більше не використовується (пішов у супплемент)

Після цього рисунки в docx треба перевставити з `paper/figures/` — у поточній версії
там лежать PNG, які я обрізав скриптом, а не ті, що вийдуть з R.

---

## 3. Таблиці в статті мають генеруватись, а не набиратись руками

У docx таблиця робастності мала зсунуті на рядок назви розподілів
(Uniform стояв у шапці, значення 1.655 приписане Triangular тощо).
`paper/tables/T3_misspecification.tex` і `results/tables/T6_misspecification.csv`
при цьому правильні — тобто помилка з'явилась при ручному перенесенні в Word.

- [ ] додати в `scripts/07_generate_paper_tables.R` експорт у .docx або .md
      (`flextable`/`officer` або хоча б `knitr::kable(format = "pipe")`),
      щоб таблиці в статтю копіювались з одного джерела
- [ ] звірити решту таблиць статті з CSV перед подачею

---

## 4. Узгодити набір n

У методиці заявлено n ∈ {46, 100, 200, 500}. Стара Табл. 3 показувала лише 100 і 200 —
рецензент це б помітив. Тепер усі чотири значення видно на рисунках,
розбіжності немає. Просто не повертати таблицю назад у скороченому вигляді.

---

## 5. Гігієна git

Незакомічене:
- `submission/SZabolotnii_ITEST_paper+++ref.docx` — вихідна 14-сторінкова версія
- `submission/SZabolotnii_ITEST_paper_10p.docx` + `.pdf` — нова
- `submission/CHANGES_14p_to_10p.md`, цей файл
- `paper/SZabolotnii_ITEST_paper++.docx`
- `paper/draft_uk.md` — модифікований

Сміття, яке не має потрапити в коміт:
- [ ] `submission/_preview/` — прев'ю-сторінки, можна видаляти
- [ ] `~$*.docx` — файли блокування Word (зараз їх три)

- [ ] додати в `.gitignore`:
      ```
      ~$*.docx
      ~$*.xlsx
      submission/_preview/
      ```
- [ ] закомітити обидві версії статті + CHANGES, щоб історія скорочення лишилась

---

## 6. Перед відправкою

- [ ] відкрити 10p.docx у Word, Ctrl+A → F9 (нумерація таблиць і рисунків на полях SEQ);
      має вийти Табл. 1–2, Рис. 1–3
- [ ] перевірити обсяг у Word — 10 сторінок пораховано конвертацією LibreOffice,
      на останній сторінці ~35% вільного місця
- [ ] `source("scripts/run_all.R")` начисто, щоб переконатись, що всі числа
      в статті відтворюються з seed 20260312
