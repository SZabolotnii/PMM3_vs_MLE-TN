# Скорочення статті: 14 → 10 сторінок

Вихідний файл `SZabolotnii_ITEST_paper+++ref.docx` не змінювався.
Результат: `SZabolotnii_ITEST_paper_10p.docx` (+ PDF для перевірки).

## Видалені таблиці (дублювали рисунки)

| Було | Куди пішла інформація |
|---|---|
| Табл. 2 (теорія TN) | Рис. 1 (права панель) + повний перелік γ₄ і g₃ у тексті розд. 2.6 |
| Табл. 3 (Монте-Карло, 13×8) | Рис. 2 і Рис. 3 — там усі 24 сценарії, а не 12; повна числова таблиця → супплемент |
| Табл. 1 (якісне порівняння) | Три речення в кінці розд. 2.5 |
| Табл. 6 (збіжність, усе 100%/0%) | Одне речення в розд. 3.3 |

Залишились: Табл. 1 (iris versicolor) і Табл. 2 (робастність).

## Рисунки

- Рис. 1 і 2 (густини TN + крива g₃) об'єднані в один двопанельний рисунок.
- Рис. 3 (гістограма залишків iris) вилучено — діагностика є числами в тексті.
- Вбудовані JPEG (785×373 px) замінено на оригінальні PNG з `paper/figures`
  з обрізаними заголовками, що були обрізані в експорті R.
  Було 5 рисунків, стало 3.

## Структура

- Розділи «Experiments» і «Results» + «Discussion» злиті в «Results and discussion»
  з трьома підрозділами (було шість дзеркальних).
- Прибрано розділ 2.7 «Conclusions», який стояв усередині Materials and methods.
- Список з 6 внесків → один абзац; Conclusions 6 пунктів → 4.
- Вилучено дубльоване рівняння g₃ (стояло і в Literature Review, і в 2.4).

## Виправлені помилки

- **Табл. робастності**: назви розподілів були зсунуті на рядок відносно чисел.
  Виправлено за `paper/tables/T3_misspecification.tex`:
  Uniform 1.655 / Triangular 0.969 / Logistic 0.994 / Student's t10 0.982.
- Розд. 2.2: «λ > √2 ≈ 0.707 — межа бімодальності» — і хибно (√2 = 1.414),
  і дублювало наступну фразу. Вилучено.
- Формула Δg₃ мала зайву дужку: `(g₃emp − g₃theor)/g₃theor)×100%`. Виправлено.
- `g₃теор` — кириличний індекс усередині формули → `g₃ theor`.
- «±10» без знака відсотка → «±10%».
- Посилання на «hypothesis H1», яка в статті ніде не формулюється. Вилучено.
- Обірваний підпис Рис. 5 («…efficiencies of PMM3»). Дописано.
- Кешовані номери полів SEQ для таблиць виставлено на 1 і 2.
- Дрібне: «large n values of λ», «0,570» → «0.570», українські «і» в англійському
  тексті, «replics».

## Що треба зробити руками

1. **Супплемент.** Текст двічі посилається на supplementary material
   (повна числова таблиця 24 сценаріїв + Табл. 6 + whiteside та чотири інші датасети).
   Треба викласти на Zenodo/GitHub і вставити DOI.
2. **Відкрити у Word і оновити поля** (Ctrl+A, F9) — нумерація таблиць і рисунків
   на полях SEQ. Перевірити, що вийшло Табл. 1–2 і Рис. 1–3.
3. **Перевірити обсяг у Word.** 10 сторінок пораховано конвертацією LibreOffice;
   Word верстає трохи інакше. На останній сторінці лишається ~35% вільного місця.
4. У методиці заявлено n ∈ {46, 100, 200, 500} — тепер це підтверджено рисунками
   (раніше таблиця показувала лише 100 і 200).

Резерв, якщо в Word не влізе: зменшити Рис. 2 і 3 з 10.0 см до 9.5 см ширини
(−0.4 стор.), або перевести таблицю робастності в текст (−0.15 стор.).

---

## Виправлення 2026-09-19 (після перевірки відтворюваності)

Причина: `results/tables/T6_misspecification.csv` і `T7_bimodal_test.csv` були
пораховані заглушкою PMM3 (повертала OLS). Після чистого прогону `run_all.R`
вони замінені на справжні результати. Подробиці в `TODO_ITEST_submission.md`, розд. 0.
Обсяг не змінився: 10 сторінок, розбиття по сторінках те саме (LibreOffice).

- **Abstract**: «PMM3 is shown to be robust …» → «Under misspecification PMM3 never
  falls below OLS (ARE from 1.02 to 2.68), whereas MLE-TN can be less efficient than OLS».
- **Вступ**: рівняння-об'єкт MathType «λ ≫ 0.707» замінено на рівняння Word «λ ≫ 1».
- **Внесок (iii)**: «maintains ARE ≈ 1» → «does not fall below OLS … (ARE from 1.02 to 2.68)».
- **Розд. 2.2, 2.6, підпис Рис. 1**: межа бімодальності λ > 1/√2 → **λ > 1**.
- **Рис. 1–3** перевставлено з `paper/figures/` (вихід R, без заголовків; на Рис. 1
  лінія межі тепер на λ = 1; на Рис. 2 підпис теоретичного g₃ — один на панель).
- **Розд. 3.2, iris**: g₃ theor = 0.517 → **0.483** (0.517 — це частка зниження).
- **Розд. 3.2, negative example**: «four further … g₃ emp ∈ [0.97, 1.03]» →
  «Five further … g₃ emp ∈ [1.03, 1.36] and LOO-MSE PMM3/OLS ≤ 1.013».
- **Табл. 2**, ARE(PMM3): 1.000 ×4 → 2.685 / 1.061 / 1.023 / 1.047.
- **Розд. 3.3**: абзац про робастність переписано під нові числа; тест збіжності —
  M = 200 (було 500), 2.8–3.7 ітерації, |bias| < 0.003 (було 0.022).
- **Conclusions**: п. «Robustness» — без «ARE ≈ 1»; п. «Empirical threshold» —
  «equivalent to OLS» → «no practical advantage over OLS (prediction error within 2%)».
  Прибрано випадковий символ табуляції всередині цього речення.

Ті самі правки внесено в `paper/draft_uk.md`. Вихідний 14-сторінковий
`SZabolotnii_ITEST_paper+++ref.docx` і файли ITONT2026 не змінювалися.
- **Розд. 3.1** (додано пізніше того ж дня): «the full numeric table is provided as
  supplementary material» → «the full numeric table, the code and all stored results are
  available at https://doi.org/10.5281/zenodo.22849684». Обсяг — 10 сторінок.
